#!/usr/local/bin/bash

# This tile should be used for various completion scripts that are populated dynamically

autoload -U +X bashcompinit && bashcompinit
source /usr/local/opt/git-extras/share/git-extras/git-extras-completion.zsh
