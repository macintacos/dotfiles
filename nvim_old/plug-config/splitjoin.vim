" Plug 'AndrewRadev/splitjoin.vim'
" Description: a better split/join that takes into account languages

" mappings
" unset original keybindings (real ones are set in vim-which-key.vim)
let g:splitjoin_split_mapping = ''
let g:splitjoin_join_mapping = ''
